import { RenderOrSpinnerDirective } from './render-or-spinner.directive';

describe('RenderOrSpinnerDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderOrSpinnerDirective();
    expect(directive).toBeTruthy();
  });
});
