import chai, { expect } from 'chai';
import sinon from 'sinon';
import sinonChai from 'sinon-chai';
import { AbstractPokemonRepository } from '../../domain/repositories/pokemon.repository';
chai.use(sinonChai);

// describe('PokemonsInStoreController - Get Pokemon Details', () => {

//     it ('should return a 200 status code if no error are caught', () => {
//         sinon.createStubInstance(AbstractPokemonRepository)
//     })
// }

